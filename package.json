{
  "name": "mnscloud-website",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 4321",
    "start": "astro dev --host 0.0.0.0 --port 4321",
    "cms": "npx decap-server",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0 --port 4321",
    "check:responsive": "node scripts/responsive-check.mjs",
    "check:domains": "node scripts/domain-boundary-check.mjs",
    "check": "npm run build && npm run check:domains && npm run check:responsive && npm run check:cookies",
    "check:cookies": "node scripts/cookie-consent-check.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.3.3"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "typescript": "^5.9.2"
  }
}
